# node-refresh-token-example

## Getting started

- `yarn` to install all project dependencies
- `docker-compose up -d` to start database service (postgreSQL)
- `yarn dev` to start the nodejs runtime
